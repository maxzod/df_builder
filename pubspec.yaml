name: df_builder
description: small package to generate code in a simple way
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.14.4 <3.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
dependencies:
  equatable: ^2.0.3
  recase: ^4.0.0
